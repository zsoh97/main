package seedu.volant.commons.logic.commands;

/**
 * Represents the result of command execution where command is to refresh a page.
 */
public class RefreshCommandResult extends CommandResult {

    public RefreshCommandResult(String feedbacktouser) {
        super(feedbacktouser);
    }
}
